// Array en memoria que guarda cada búsqueda que no encontró resultados.
// Se reinicia cada vez que se reinicia el servidor (es esperable, es solo para la demo).
const searchMetrics = [];

module.exports = searchMetrics;
